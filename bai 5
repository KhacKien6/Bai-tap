#include <stdio.h>
#include <math.h>


int main(){
	int a;
	scanf ("%d",&a);
	
	if(((a % 4 ==0) && (a % 100 != 0)) || (a%400==0)){
		printf ("Day la nam nhuan!");
	}
	else {
		printf ("Day khong phai la nam nhuan!");
	}
	
	return 0;
}
